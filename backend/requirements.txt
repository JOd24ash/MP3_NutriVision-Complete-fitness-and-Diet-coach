fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pydantic
email-validator
python-multipart

# auth
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1        # newer bcrypt (4.1+) breaks passlib's backend detection

# vision (optional until a fine-tuned checkpoint exists — app/vision falls
# back to a mock detector without these)
ultralytics
torch
torchvision
pillow

# speech (optional until configured — app/speech falls back to a mock
# transcriber without this)
openai-whisper

# rag
chromadb
anthropic          # or swap for your LLM provider's SDK in app/rag/chat.py

# dev
alembic
pytest
httpx
